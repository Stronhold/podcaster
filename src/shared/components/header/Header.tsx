import { Card, CardContent, Typography } from '@mui/material';

export const Header = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant='h5' color={'primary'}>
          Podcaster
        </Typography>
      </CardContent>
    </Card>
  );
};

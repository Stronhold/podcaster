import { SxProps } from '@mui/material';

interface Style {
  container: SxProps;
}
export const styles: Style = {
  container: {
    display: 'flex',
    gap: 2,
    flexDirection: 'column',
    flex: '1',
  },
};
